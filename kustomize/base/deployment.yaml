apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  namespace: default
spec:
  replicas: 1
  template:
    metadata:
    spec:
      containers:
      - name: dev-app
        image: ttl.sh/my-dev-app:1d
